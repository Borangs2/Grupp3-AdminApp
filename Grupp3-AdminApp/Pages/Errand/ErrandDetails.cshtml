@page
@model Grupp3_Elevator.Pages.Errand.ErrandDetailsModel
@{
}
<section>
    <div class="container">
        <h1 class="mb-5">
            Errand Details
            <a asp-page="/Elevator/ElevatorDetails" asp-route-elevatorId="@Model.Elevator.Id" class="btn btn-primary">Return</a>
            <a asp-page="/Errand/ErrandEdit" asp-route-elevatorId="@Model.Elevator.Id" asp-route-errandId="@Model.Errand.Id" class="btn btn-success">Edit</a>
        </h1>
        <div class="row mb-5">
            <div class="col-6">
                <div class="container card">
                    <h4>Info</h4>
                    <table class="table my-auto">
@*                        <tr>
                            <th>Id:</th>
                            <td>@Model.Errand.Id</td>
                        </tr>*@
                        <tr>
                            <th>Title:</th>
                            <td>@Model.Errand.Title</td>
                        </tr>
                        <tr>
                            <th>Status:</th>
                            <td>@Model.Errand.Status</td>
                        </tr>
                        <tr>
                            <th>Last Edited</th>
                            <td>@Model.Errand.LastEdited</td>
                        </tr>
                        <tr>
                            <th>Created At:</th>
                            <td>@Model.Errand.CreatedAt</td>
                        </tr>
                        <tr>
                            <th>Created By:</th>
                            <td>@Model.Errand.CreatedBy</td>
                        </tr>
                        <tr>
                            <th>Technician:</th>
                            <td>@Model.Errand.Technician.Name</td>
                        </tr>
                    </table>
                </div>
            </div>
            <div class="col-6">
                <div class="container card">
                    <h4>Description</h4>
                    <p>@Model.Errand.Description</p>
                </div>
            </div>

        </div>
        <div class="col">
            <div class="container card">
                <table class="table">
                    @foreach (var comment in Model.Errand.Comments.OrderByDescending(a => a.PostedAt))
                    {
                        <tr class="d-flex flex-column">
                            <th>Posted At: @comment.PostedAt - Author: @comment.Author</th>
                            <td>@comment.Content</td>
                        </tr>
                    }
                </table>
                <div class="mx-auto">
                    <form method="post">
                        <div class="form-group">
                            <label asp-for="ChosenSelectTechnician">Technician</label>
                            <select class="form-control" asp-for="ChosenSelectTechnician" asp-items="@Model.SelectTechnician"></select>
                            <span asp-validation-for="ChosenSelectTechnician" class="field-validation-error"></span>
                        </div>
                        <div class="form-group text-center">
                            <textarea asp-for="Content" class="form-control mt-3" placeholder="Write comment here..." cols="70" rows="4"></textarea>
                            <button asp-route-elevatorId="@Model.Elevator.Id" asp-route-errandId="@Model.Errand.Id" class="mt-3 mb-3 btn btn-primary" type="submit">Save</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>